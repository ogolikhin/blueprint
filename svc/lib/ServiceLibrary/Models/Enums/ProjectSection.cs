namespace ServiceLibrary.Models.Enums
{
    // The content is copied from the Raptor solution.
    // KEEP IN SYNC!

    /// <summary>
    /// These values are also used in SQL code
    /// </summary>
    public enum ProjectSection
    {
        Unknown = 0,
        Artifacts = 1,
        BaselinesAndReviews = 2,
        Collections = 3
    }
}
